[workspace]
members = [
    "erc20",
    "example/erc20-token",
    "example/erc20-tests"
]
default-members = [
    "erc20",
    "example/erc20-tests"
]

[profile.release]
codegen-units = 1
lto = true
